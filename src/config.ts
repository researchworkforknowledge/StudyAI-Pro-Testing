// StudyAI Pro configuration for static sites (like GitHub Pages)
export const STATIC_CONFIG = {
  // If you are deploying to GitHub Pages and want all your users to use your AI features
  // without having to enter their own API key, paste your Gemini API Key here.
  // Leave empty ("") if you prefer asking users to enter their own key.
  GEMINI_API_KEY: "YOUR_COPIED_GEMINI_API_KEY_HERE"
};
